[package]
name = "syswatch"
version = "0.1.0"
description = "macOS system monitor"
authors = ["syswatch contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

